# Minimal Dockerfile for Node 22 devcontainer (no Python)
FROM mcr.microsoft.com/devcontainers/base:ubuntu-22.04

# Keep the image minimal: do not install Python or extra build tools here.
# The devcontainer 'node' feature will provide Node 22 at build time.
USER vscode
